/// <reference types="node" />
import { Duplex, DuplexOptions } from 'stream';
export declare class OggDemuxer extends Duplex {
    private remaining?;
    private opus_head?;
    private ogg_head?;
    constructor(options?: DuplexOptions);
    _read(): void;
    _write(chunk: Buffer, encoding: BufferEncoding, done: (error?: Error) => void): void;
    private readOggPage;
    private readOggData;
    private readOggHead;
    _destroy(err: Error | null, callback: (error: Error | null) => void): void;
    _final(callback: (error?: Error) => void): void;
    _cleanup(): void;
}
//# sourceMappingURL=OggDemuxer.d.ts.map