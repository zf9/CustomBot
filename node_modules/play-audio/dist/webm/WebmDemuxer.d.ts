/// <reference types="node" />
import { Duplex, DuplexOptions } from "stream";
import { WebmHeader } from "./WebmHeader";
export declare class WebmDemuxer extends Duplex {
    remaining?: Buffer;
    chunk?: Buffer;
    cursor: number;
    header: WebmHeader;
    headfound: boolean;
    private data_size;
    private data_length;
    constructor(options: DuplexOptions);
    private get vint_length();
    private get vint_value();
    cleanup(): void;
    _read(): void;
    _write(chunk: Buffer, _: BufferEncoding, done: (error?: Error | null) => void): void;
    private readTag;
    private parseEbmlID;
    _destroy(error: Error | null, callback: (error: Error | null) => void): void;
    _final(callback: (error?: Error | null) => void): void;
}
//# sourceMappingURL=WebmDemuxer.d.ts.map